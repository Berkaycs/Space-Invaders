using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable] // convert data format to convenient format to store in database
public class Character // We won't be attach this script to any gameobject so you can delete monobehavior
{
    public Sprite characterSprite;
}
